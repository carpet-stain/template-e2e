name: Epic complete

# Thin caller — the body lives in the template source repo's
# reusable-epic-complete.yml (its ADR-0004); Dependabot bumps the SHA pin.
on:
  issues:
    types: [closed]

permissions:
  issues: write

jobs:
  epic:
    uses: carpet-stain/project-starter-template/.github/workflows/reusable-epic-complete.yml@504d81a0dca5b0e67d7756610cf22b49b2be7ddf # main
