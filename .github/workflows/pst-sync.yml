name: Sync from project-starter-template

# Thin caller — pinned to the implementor branch for pre-merge live
# verification of project-starter-template#147; reverted after verification.
on:
  workflow_dispatch:

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  pst-sync:
    uses: carpet-stain/project-starter-template/.github/workflows/reusable-sync.yml@147-sync-channel
    secrets: inherit
