# template-e2e

Standing sandbox for project-starter-template's live e2e. Rendered
git-flow output is pushed, verified, and discarded per run; not a real
project. See carpet-stain/infra for the governing OpenTofu config.
